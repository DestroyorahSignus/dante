# Notebooks

Placeholder. The following Kaggle/Colab notebooks are part of the DANTE workflow
(see DANTE_BUILD_PLAN.md §2). They are not checked in yet — create them as `.ipynb`
when running on a GPU host:

- `01_data_exploration.ipynb` — ESCI dataset exploration
- `02_train_biencoder.ipynb` — train the ModernBERT bi-encoder (Kaggle/Colab)
- `03_train_splade.ipynb` — fine-tune / download SPLADE-v3
- `04_eval_and_ablation.ipynb` — run the eval pipeline and build the ablation table
